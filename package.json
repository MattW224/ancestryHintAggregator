{
  "name": "ancestry-member-info",
  "version": "1.0.0",
  "description": "Show date, not time passed, since user last visited.",
  "main": "content.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.38.0",
    "eslint-config-google": "^0.14.0"
  }
}
